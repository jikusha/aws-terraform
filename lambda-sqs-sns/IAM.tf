// IAM - Identity and Access Management
// IAM users and groups
// users are people in the organization
// groups can be deifferent team in a organization
// users can belong to multiple groups (or it may not belong to any group)
// users or groups can be attached with IAM policies which give them the permission
// we need to give least priviledge


// IAM Roles
// Roles need to be attached with services to perform action
// e.g. Glue Role, Lambda Role etc.